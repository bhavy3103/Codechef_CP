#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int fa;
        int a[3], ct = 0, sum = 0;
        for (int i = 0; i < 3; i++)
        {
            cin >> a[i];
            sum = sum + a[i];
        }
        if (sum > 3)
    }
    return 0;
}